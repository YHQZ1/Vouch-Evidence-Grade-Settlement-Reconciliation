# Development fixtures

These small JSON fixtures are intentionally readable examples for reviewers and
future Phase 4 control tests. They are not the demonstration or held-out source
batches. Each case includes a compact evidence snapshot and its expected outcome;
the frozen batches remain the broader source of scenario coverage.
